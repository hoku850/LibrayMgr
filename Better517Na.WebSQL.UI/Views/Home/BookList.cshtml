@{
    Layout = null;
}
@model List<Better517Na.LibrayMgr.Models.DisplayInfo>
@if (Model != null)
{
    <table class="table table-hover">
        <tr class="success font">
            <td class="font">ID</td>
            <td class="font">书名</td>
            <td class="font">描述</td>
            <td class="font">藏书总数</td>
            <td class="font">可借数</td>
        </tr>
 @{
    int i = 0;
    foreach (Better517Na.LibrayMgr.Models.DisplayInfo displayInfo in Model)
    {
        string styledata = ((i % 2) == 0 ? "double" : "single");
            <tr class = "active font")>
                <td class="font">@displayInfo.BookID</td>
                <td class="font">@displayInfo.Title</td>
                <td class="font">@displayInfo.Decrible</td>
                <td class="font">@displayInfo.BookNum</td>
                <td class="font">@displayInfo.Bookleft</td>
            </tr>
        i++;
    }
 }
    </table>
}